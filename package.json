{
    "name": "simba-task-manager",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "engines": {
        "node": "18.x"
    },
    "scripts": {
        "build": "cd client && npm run build",
        "start": "cd client && npm run preview"
    }
}